### Refresh token
### Revokes old token, returns new access + refresh pair.

POST http://localhost:8080/api/v1/auth/refresh-token
Content-Type: application/json

{
  "refreshToken": "{{refresh_token}}"
}
